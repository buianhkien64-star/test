/**
 * Enum representing the different player character types
 */
public enum PlayerType {
    DEFAULT,  // No character selected yet
    ROBOT,    // Robot character with +5 coins per kill
    MARINE    // Marine character with river immunity
}
